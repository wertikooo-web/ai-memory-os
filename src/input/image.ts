import type { ImageInput, NormalizedInput } from "./types.js";

export async function normalizeImage(_input: ImageInput): Promise<NormalizedInput> {
  throw new Error("Image input normalization is TODO: add OCR or vision analysis later.");
}