import type { DocumentInput, NormalizedInput } from "./types.js";

export async function normalizeDocument(_input: DocumentInput): Promise<NormalizedInput> {
  throw new Error("Document input normalization is TODO: add text extraction later.");
}