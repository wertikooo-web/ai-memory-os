import type { LinkInput, NormalizedInput } from "./types.js";

export async function normalizeLink(_input: LinkInput): Promise<NormalizedInput> {
  throw new Error("Link input normalization is TODO: add page metadata and main text extraction later.");
}