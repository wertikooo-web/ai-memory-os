import type { MorningFocusParams } from "./types.js";

export async function buildMorningFocus(_params: MorningFocusParams): Promise<never> {
  throw new Error("buildMorningFocus is planned for a later MVP checkpoint.");
}